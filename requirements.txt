# ================================
# WEB FRAMEWORK Y SERVIDOR
# ================================

# FastAPI - Framework web moderno para crear APIs REST
# Características: validación automática, documentación auto-generada, type hints
fastapi==0.115.0

# Uvicorn - Servidor ASGI de alto rendimiento para ejecutar aplicaciones FastAPI
# [standard] incluye extras: watchfiles (auto-reload), websockets, colorama (logs coloridos)
uvicorn[standard]==0.30.0


# ================================
# TESTING Y DESARROLLO
# ================================

# pytest - Framework de testing moderno y potente
# Más simple que unittest, mejor output de errores, fixtures potentes
pytest==8.3.0

# pytest-xdist - Plugin para ejecutar tests en paralelo
# Acelera significativamente la ejecución de tests usando múltiples workers
pytest-xdist==3.8.0

# pytest-asyncio - Plugin para testing de código asíncrono
# Permite ejecutar tests async/await y fixtures asíncronos
pytest-asyncio==0.24.0

# httpx - Cliente HTTP para testar APIs FastAPI
# Permite hacer requests HTTP en tests de integración
httpx==0.27.0


# ================================
# SEGURIDAD Y CRIPTOGRAFÍA
# ================================

# bcrypt - Librería para hashing seguro de passwords
# Implementa algoritmo bcrypt con salt automático
bcrypt==4.1.2
